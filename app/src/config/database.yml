default: &default
  adapter: postgresql
  encoding: unicode
  pool: 16
  username: docker
  password: docker
    host: <%= ENV["DISCOVERY_DNS_PREFIX"] %>db
  port: 5432

development:
  <<: *default
  database: docker
  host: <%= ENV["DISCOVERY_DNS_PREFIX"] %>db

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: minichat_test

production:
  <<: *default
  database: minichat
