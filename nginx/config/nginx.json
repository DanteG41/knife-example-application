{
  "service": {
    "name": "${DISCOVERY_DNS_PREFIX}_nginx",
    "tags": [
      "http",
      "http:${SUB_DOMAIN}.${DOMAIN}"
    ],
    "port": 80,
    "check": {
      "script": "curl -c1 localhost:80 > /dev/null",
      "interval": "10s"
    }
  }
}
